using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WaterFlowNextTube : MonoBehaviour
{
    //[SerializeField] private WaterFlow refToPrevTube;
    //[Range(0, 16)]
    //public float Speed;
    //private float _pipeScale = 1;
    //private float Pressure, RoundedPressure, Flow, RoundedFlow, pi = 3.14f, d, RoundedSpeed;
    //private void Update()
    //{
    //    d = _pipeScale * 100;
    //    Speed = (refToPrevTube.Speed * refToPrevTube.GetD() * refToPrevTube.GetD()) / (d * d);
    //    Pressure = (refToPrevTube.GetPressure() + 0.5f * refToPrevTube.Speed) / Speed - 1;
    //}
}
